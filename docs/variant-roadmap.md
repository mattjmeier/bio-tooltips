# Variant Tooltips Roadmap

Variant tooltips are planned as a future tooltip module.

Likely scope:

- variant identifiers and normalized coordinates
- transcript and gene context
- clinical significance summaries
- links to relevant variant resources

The expected shape is a `VariantTooltip` module with its own adapter and package subpath, matching the model used by gene and chemical tooltips.
